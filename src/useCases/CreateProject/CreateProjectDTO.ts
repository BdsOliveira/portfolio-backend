export interface CreateProjectDTOInterface {
    title: string;
    objective: string;
    features: string[];
    skills: string[];
    images: string[];
    github_url: string;
    live_url: string;
    is_visible: boolean;
}